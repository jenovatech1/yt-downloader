/// Get Clip — durasi & jumlah tetap (tanpa dropdown).
class GetClipConfig {
  GetClipConfig._();

  static const minClipSec = 60;
  static const maxClipSec = 120;
  static const maxClips = 6;

  static const durationPrompt = '60-120 detik (1-2 menit)';
}
